KGRV-former Replication and Training Requirements
===============================================

System Configuration
--------------------
Operating system : Ubuntu 22.04
Python           : 3.12
PyTorch          : 2.5.1
CUDA             : 12.4
GPU              : NVIDIA RTX 4090 (24 GB)
CPU              : Intel Xeon Gold 6430
System memory    : 120 GB

Recommended Python Libraries for Model Training
-----------------------------------------------
# Core deep learning
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# NLP / transformer modeling
transformers==4.46.3
tokenizers==0.20.3
sentencepiece==0.2.0

# Dataset handling and evaluation
datasets==3.1.0
scikit-learn==1.5.2
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1

# Training utilities
tqdm==4.67.1
accelerate==1.1.1

# Visualization / logging
matplotlib==3.9.2
seaborn==0.13.2
tensorboard==2.18.0

# Optional experiment tracking
wandb==0.18.7

# Reproducibility and environment notes
- Use a fresh Python 3.12 virtual environment.
- Ensure NVIDIA drivers are compatible with CUDA 12.4.
- Verify CUDA availability in PyTorch using:
  python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
- Fix random seeds and keep dataset splits consistent across runs.
